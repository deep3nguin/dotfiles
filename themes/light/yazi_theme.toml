# theme.toml - Color scheme and style customization for Yazi file manager
# Strictly derived from the design system (DESIGN.md)

[app]
# Uses foreground (#171717) for text and parchment (#fefffc) for background
overall = { fg = "#171717", bg = "#fefffc" }

[manager]
# Highlights current directory in cerulean (#0081c0)
cwd = { fg = "#0081c0", bold = true }

# Character used to draw separation borders
border_symbol = "│"

# Layout borders use mist (#dee2de)
border_style = { fg = "#dee2de" }

# Copy badge uses primary accent (#41a1cf)
marker_copied   = { fg = "#41a1cf", bg = "#41a1cf" }
# Cut badge uses dusk (#1f1f29)
marker_cut      = { fg = "#1f1f29", bg = "#1f1f29" }
# Marked badge uses cerulean (#0081c0)
marker_marked   = { fg = "#0081c0", bg = "#0081c0" }
# Multi-select badge uses primary accent (#41a1cf)
marker_selected = { fg = "#41a1cf", bg = "#41a1cf" }

# Copied count badge uses primary accent (#41a1cf) text on parchment (#fefffc)
count_copied   = { fg = "#41a1cf", bg = "#fefffc" }
# Cut badge uses dusk (#1f1f29) text on parchment (#fefffc)
count_cut      = { fg = "#1f1f29", bg = "#fefffc" }
# Selected count badge uses primary accent (#41a1cf) text on parchment (#fefffc)
count_selected = { fg = "#41a1cf", bg = "#fefffc" }

[indicator]
# Active cursor uses paper text on dusk (#1f1f29) background (filled button style)
current = { fg = "#ffffff", bg = "#1f1f29", bold = true }

# Parent cursor uses foreground (#171717) text on linen (#f9faf7) background
parent  = { fg = "#171717", bg = "#f9faf7" }

# Underlines the cursor position in the preview pane
preview = { underline = true }

[status]
# Status line sits on paper (#ffffff) with foreground (#171717) text
overall = { fg = "#171717", bg = "#ffffff" }

# Normal mode indicator uses paper text on dusk (#1f1f29) background
mode_normal = { fg = "#ffffff", bg = "#1f1f29", bold = true }

# Select mode indicator uses foreground (#171717) text on mist (#dee2de) background
mode_select = { fg = "#171717", bg = "#dee2de", bold = true }

# Unset/other modes use paper text on dusk (#1f1f29) background
mode_unset  = { fg = "#ffffff", bg = "#1f1f29", bold = true }

# Progress labels use bold foreground (#171717) text
progress_label  = { fg = "#171717", bold = true }
# Active progress bar uses primary accent (#41a1cf) on paper (#ffffff) track
progress_normal = { fg = "#41a1cf", bg = "#ffffff" }
# Error progress bar uses dusk (#1f1f29) on linen (#f9faf7) track
progress_error  = { fg = "#1f1f29", bg = "#f9faf7" }

# Permission separators use mist (#dee2de)
perm_sep  = { fg = "#dee2de" }
# Permission directory indicator uses cerulean (#0081c0)
perm_type = { fg = "#0081c0" }
# Read permissions use ash (#646464)
perm_read = { fg = "#646464" }
# Write permissions use dusk (#1f1f29)
perm_write= { fg = "#1f1f29" }
# Execute permissions use primary accent (#41a1cf)
perm_exec = { fg = "#41a1cf" }

[input]
# Focused input border glows with primary accent (#41a1cf)
border   = { fg = "#41a1cf" }
title    = {}
value    = {}
# Text selection in inputs uses linen (#f9faf7)
selected = { bg = "#f9faf7" }

[pick]
# Dropdowns use mist border (#dee2de)
border   = { fg = "#dee2de" }
# Active items in dropdown lists use primary accent (#41a1cf)
active   = { fg = "#41a1cf", bold = true }
# Inactive dropdown items inherit default styling
inactive = {}

[completion]
# Autocomplete popup borders use mist border (#dee2de)
border   = { fg = "#dee2de" }
# Active autocomplete selection uses cerulean (#0081c0) on linen (#f9faf7)
active   = { fg = "#0081c0", bg = "#f9faf7", bold = true }
# Inactive autocomplete items inherit default styling
inactive = {}

[tasks]
# Task list borders use mist border (#dee2de)
border = { fg = "#dee2de" }
# Task list title uses bold foreground (#171717)
title  = { fg = "#171717", bold = true }
# Hovering a task uses paper text on dusk (#1f1f29) background
hover  = { fg = "#ffffff", bg = "#1f1f29" }

[which]
# Key chord panel uses parchment (#fefffc)
mask = { bg = "#fefffc" }
# Key candidates use primary accent (#41a1cf)
cand = { fg = "#41a1cf" }
# Remaining keys in layout use ash (#646464)
rest = { fg = "#646464" }
# Descriptions use cerulean (#0081c0)
desc = { fg = "#0081c0" }
# Separator string between key and description
separator = " ➔ "
# Separator style uses mist border (#dee2de)
separator_style = { fg = "#dee2de" }

[help]
# Help keys use primary accent (#41a1cf)
on   = { fg = "#41a1cf" }
# Help execution command uses cerulean (#0081c0)
run  = { fg = "#0081c0" }
# Help descriptions use foreground (#171717)
desc = { fg = "#171717" }
# Hovering a help item highlights in paper text on dusk (#1f1f29) background
hover = { fg = "#ffffff", bg = "#1f1f29" }

[filetype]
# Defines syntax highlighting styles based on file extensions or mime-types
rules = [
  # Directories use cerulean (#0081c0) and bold formatting
  { url = "*/", fg = "#0081c0", bold = true },
  
  # Media files use primary accent (#41a1cf)
  { mime = "image/*", fg = "#41a1cf" },
  { mime = "video/*", fg = "#41a1cf" },
  { mime = "audio/*", fg = "#41a1cf" },
  
  # Archives and compressed files use cerulean (#0081c0)
  { mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#0081c0" },
  
  # Text files use standard foreground (#171717)
  { mime = "text/*", fg = "#171717" },
  
  # Documents/PDFs use ash (#646464)
  { mime = "application/{pdf,epub+zip,postscript,x-shockwave-flash}", fg = "#646464" },

  # Fallback for all other files uses foreground (#171717)
  { mime = "*", fg = "#171717" }
]
