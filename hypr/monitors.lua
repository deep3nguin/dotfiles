-- Monitor configuration for Hyprland (aligned with DESIGN.md layout system)

-- Auto-detect all connected monitors and position them automatically using preferred resolutions
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = 1 }) -- Set default monitor configuration with automatic scaling
